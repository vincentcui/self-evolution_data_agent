# 环境变量模板
# 复制为 .env 并填写实际值
#
# LLM 模型配置: 不再通过 env var 管理, 改为 Web UI (模型管理页面) 添加激活.
# 首次启动后访问 Web UI → 模型管理 → 添加 CHAT + EMBEDDING 配置 → 激活即可.

# Git 仓库访问令牌 (可选，私有仓库需要)
# GitHub: ghp_xxxxxxxxxxxx
# GitLab: glpat-xxxxxxxxxxxx
# Gitee: xxxxxxxxxxxx
IS_GIT_TOKEN=

# 测试数据库 URL (pytest 专用，库名须以 _test 结尾；CI 由 workflow env 注入)
# 本地跑测试时由 backend/.env 自动加载，无需手动 export
IS_TEST_DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/self_evolution_data_agent_test
